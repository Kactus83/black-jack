/**
 * Interface représentant un joueur générique.
 * Utilisée avant que le jeu ne démarre.
 */
export interface Player {
  id: string;          // Identifiant unique du joueur
  nickname: string;    // Pseudo du joueur
}
